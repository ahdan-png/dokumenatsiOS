'use client';import {useState} from 'react';export default function LoginForm({role}:{role:'panitia'|'pdd'|'admin'}){const [username,setUsername]=useState(''),[password,setPassword]=useState(''),[showPassword,setShowPassword]=useState(false),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setError('');try{const r=await fetch('/api/'+role+'/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(role==='panitia'?{password}:{username,password})});if(r.ok)location.href=role==='admin'?'/admin/dashboard':role==='pdd'?'/pdd/menu':'/panitia';else setError((await r.json()).error||'Login gagal.')}catch{setError('Server tidak dapat dihubungi. Pastikan alamat website dan koneksi WiFi benar.')}}return <form onSubmit={submit} className="mx-auto w-full max-w-sm space-y-5 rounded-2xl bg-white p-6 shadow-xl md:p-8"><div className="text-center"><h1 className="text-2xl font-bold text-slate-900">DOKUMENTASI OSIM</h1><p className="mt-1 text-sm text-slate-500">Masuk untuk melanjutkan</p></div>{role!=='panitia'&&<input className="w-full rounded-xl border border-slate-200 px-4 py-3 outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20" value={username} onChange={e=>setUsername(e.target.value)} placeholder="Username" autoComplete="username" required/>}<div className="relative"><input className="w-full rounded-xl border border-slate-200 px-4 py-3 pr-20 outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-500/20" type={showPassword?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" autoComplete="current-password" required/><button type="button" aria-label={showPassword?'Sembunyikan password':'Tampilkan password'} onClick={()=>setShowPassword(!showPassword)} className="absolute right-3 top-1/2 -translate-y-1/2 rounded-full px-2 py-1 text-sm text-blue-600 hover:bg-blue-50">{showPassword?'◉':'◌'}</button></div><button className="flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-blue-600 to-blue-500 py-3 font-medium text-white shadow-md transition hover:from-blue-700 hover:to-blue-600"><span aria-hidden="true">→</span>Masuk</button>{error&&<p className="text-sm text-red-600">{error}</p>}</form>}
