'use client';import {useState} from 'react';export default function LoginForm({role}:{role:'panitia'|'pdd'|'admin'}){const [username,setUsername]=useState(''),[password,setPassword]=useState(''),[showPassword,setShowPassword]=useState(false),[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setError('');try{const r=await fetch('/api/'+role+'/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(role==='panitia'?{password}:{username,password})});if(r.ok)location.href=role==='admin'?'/admin/dashboard':role==='pdd'?'/pdd/dashboard':'/panitia';else setError((await r.json()).error||'Login gagal.')}catch{setError('Server tidak dapat dihubungi. Pastikan alamat website dan koneksi WiFi benar.')}}return <form onSubmit={submit} className="glass mx-auto max-w-sm space-y-4 rounded-2xl p-7 shadow-xl">{role!=='panitia'&&<input className="w-full rounded border p-3" value={username} onChange={e=>setUsername(e.target.value)} placeholder="Username" autoComplete="username" required/>}<div className="relative"><input className="w-full rounded border p-3 pr-20" type={showPassword?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" autoComplete="current-password" required/><button type="button" onClick={()=>setShowPassword(!showPassword)} className="absolute right-2 top-1/2 -translate-y-1/2 px-2 text-sm text-slate-600">{showPassword?'Sembunyikan':'Lihat'}</button></div><button className="w-full rounded bg-sky-700 py-3 font-semibold text-white">Masuk</button>{error&&<p className="text-sm text-red-600">{error}</p>}</form>}
