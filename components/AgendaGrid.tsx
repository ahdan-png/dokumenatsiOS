import Link from 'next/link';
type Agenda={slug:string;title:string;description:string;link:string|null;date?: string|Date|null};
export default function AgendaGrid({section,agendas,hrefBase}:{section:string;agendas:Agenda[];hrefBase?:string}){const base=hrefBase||`/${section}`;return <main className="min-h-[calc(100vh-73px)] bg-slate-100 p-6 md:p-12"><div className="mx-auto max-w-5xl"><h1 className="mb-2 text-4xl font-bold capitalize">{section}</h1><p className="mb-8 text-slate-600">Pilih agenda untuk melihat detail dokumentasi.</p>{agendas.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{agendas.map(a=><Link href={`${base}/${a.slug}`} key={a.slug} className="glass rounded-2xl p-6 transition hover:-translate-y-1 hover:shadow-xl"><h2 className="text-xl font-semibold">{a.title}</h2>{a.date&&<p className="mt-1 text-sm text-slate-500">{new Date(a.date).toLocaleDateString('id-ID')}</p>}<p className="mt-2 line-clamp-3 text-sm text-slate-600">{a.description||'Lihat detail agenda'}</p><span className="mt-5 inline-block text-sm font-semibold text-sky-700">Lihat detail →</span></Link>)}</div>:<div className="glass rounded-2xl p-8 text-slate-500">Belum ada agenda.</div>}</div></main>}
