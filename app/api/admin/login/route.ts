import {NextResponse} from 'next/server';import bcrypt from 'bcryptjs';import {db} from '@/lib/db';import {setSession} from '@/lib/auth';import {allowed} from '@/lib/rateLimit';import {credentials} from '@/lib/validation';
export async function POST(req:Request){const ip=req.headers.get('x-forwarded-for')||'unknown';if(!allowed('admin:'+ip))return NextResponse.json({error:'Terlalu banyak percobaan. Coba lagi nanti.'},{status:429});try{const {username,password}=credentials.parse(await req.json());const user=await db.adminUser.findUnique({where:{username}});if(!user||!(await bcrypt.compare(password,user.passwordHash)))return NextResponse.json({error:'Username atau password salah'},{status:401});setSession('admin',user.id);return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Data tidak valid'},{status:400})}}
