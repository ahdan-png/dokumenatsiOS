import {NextResponse} from 'next/server';import {db} from '@/lib/db';export const dynamic='force-dynamic';export async function GET(_:Request,{params}:{params:{slug:string}}){const section=await db.section.findUnique({where:{slug:params.slug},include:{agendas:{orderBy:{createdAt:'asc'}}}});return section?NextResponse.json({slug:section.slug,name:section.name,link:section.link,description:section.description,wallpaperUrl:section.wallpaperUrl,agendas:section.agendas},{headers:{'Cache-Control':'no-store'}}):NextResponse.json({error:'Section tidak ditemukan'},{status:404})}
