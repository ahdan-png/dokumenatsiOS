import {getSession} from '@/lib/auth';import LogoutButton from '@/components/LogoutButton';import DashboardMenu from '@/app/dashboard-menu';import {redirect} from 'next/navigation';
export default function AdminDashboard(){if(getSession()?.role!=='admin')redirect('/admin');return <main className="mx-auto max-w-5xl space-y-8 p-6 md:p-8"><div className="flex items-center justify-between"><div><p className="text-sm text-gray-500">ADMIN</p><h1 className="text-2xl font-medium text-black">Kelola Dokumentasi</h1></div><LogoutButton/></div><DashboardMenu basePath="/admin/dashboard"/></main>}
